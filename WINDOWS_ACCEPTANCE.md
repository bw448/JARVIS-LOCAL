# Windows 集成与性能验收

这份验收用于区分三件事：源码功能是否正确、离线发布包是否真的包含新版本、真实 Windows 电脑上的麦克风与响应速度是否达标。

## 1. 先运行只读诊断

在项目的 Python 3.11 或 3.12 环境中执行：

```powershell
.\.venv\Scripts\python.exe .\scripts\windows_acceptance.py --package-dir D:\JARVIS-LOCAL
```

需要留档时执行：

```powershell
.\.venv\Scripts\python.exe .\scripts\windows_acceptance.py --package-dir D:\JARVIS-LOCAL --output .\WINDOWS_ACCEPTANCE_REPORT.md
```

脚本只读取运行环境、发布包、模型文件和本机健康端点。它不会启动应用、加载大模型、录音，也不会执行电脑控制动作。电脑控制检查只创建并取消一次内存中的操作提案。

出现 `BLOCKED` 时不要发布。`NOT_RUN` 表示对应本机服务没有启动；`MANUAL` 必须在原生 Windows 桌面逐项观察。

## 2. 人工功能验收

使用即将发布的完整离线目录，不要用源码窗口代替 EXE：

- 双击 EXE 后主窗口在 5 秒内出现，且没有后台异常弹窗；
- 将智能体名字改成“贾维斯”，主面板、语音模式和悬浮 HUD 中央仍保持抽象声纹，不显示“贾”字；
- 单次录音完成“录音 → STT → 发送 → 流式回答 → 分句朗读”的闭环；
- 连续语音能在停顿后及时识别，回答朗读完成后自动恢复监听；
- 回答过程中点击停止朗读，当前声音和后续排队句子都立即停止；
- 用“我今天特别累，什么都不想做”等表达测试陪伴感：应先承接感受，再简短询问或提供帮助，不应武断诊断情绪；
- 开启电脑控制后，让助手打开记事本和计算器：必须先出现清晰确认框；取消不执行，确认只执行一次；
- 关闭电脑控制后重复请求，任何系统操作都不应执行；
- 不测试自动锁屏，除非已经保存当前工作并明确准备好锁屏。

## 3. 性能记录

每项分别记录第一次冷启动和第二次热启动，连续测三次，使用中位数判断：

| 指标 | 推荐门槛 | 记录方式 |
|---|---:|---|
| 热启动到主窗口出现 | ≤ 5.0 秒 | 从双击 EXE 到窗口可操作 |
| 说完话到结束录音 | ≤ 1.0 秒 | 观察“聆听”切换为“识别” |
| SenseVoice CPU 短句 STT | ≤ 1.5 秒 | 开启诊断日志，读取 `stt_ms` |
| Whisper small CPU 短句 STT | ≤ 3.0 秒 | 开启诊断日志，读取 `stt_ms` |
| 本地 LLM 首字 | ≤ 1.5 秒 | 读取 `first_token_ms` |
| Kokoro CPU 首句发声 | ≤ 1.0 秒 | 读取 TTS 计时并以实际听感复核 |
| Qwen3-TTS 0.6B GPU 热态首句 | ≤ 1.2 秒 | 工作进程预热后测量 |

这些数值是产品目标，不是对所有硬件的保证。如果首字很快但首句发声仍慢，应优先定位 TTS；如果结束录音慢，应调低静音判断或检查麦克风噪声；不要把所有延迟都归因于对话模型。

## 4. 发布门槛

只有同时满足以下条件才把包标记为 0.7.0 可发布：

1. 诊断中的离线包版本与源码版本一致；
2. Kokoro、Whisper 关键模型和许可证齐全，打包时 `SELF-TEST.json` 成功；
3. Python 自动化测试与前端语法检查全部通过；
4. 原生 Windows 人工功能验收全部通过；
5. 至少在一台纯 CPU 电脑和一台 NVIDIA GPU 电脑记录性能；
6. 断网后重新启动，STT、TTS、界面和电脑控制仍按设计工作；只有文本模型是否可用取决于其是否也是本地部署。

SenseVoice 和 Qwen3-TTS/CosyVoice3 可以离线随软件分发，但不能简单塞入主 EXE：应继续采用独立模型包和本机工作进程，发布前还要逐项确认模型许可证、模型体积、CUDA 运行库、最低显存和首次加载时间。
