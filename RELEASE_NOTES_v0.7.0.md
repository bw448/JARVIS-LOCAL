# JARVIS LOCAL 0.7.0

发布前新增 `scripts/windows_acceptance.py`：自动核对源码与离线包版本、关键语音模型、打包自检、本机工作进程、GPU 状态和电脑控制取消链路。真实麦克风、GUI 与电脑操作仍须按 `WINDOWS_ACCEPTANCE.md` 在原生 Windows 上人工验收。

0.7.0 增加第一阶段 Windows 白名单电脑控制。目标不是让语言模型获得任意系统权限，而是建立可审计、可确认、可逐步扩展的工具边界。

## 当前白名单

- `get_system_status`：读取操作系统版本、CPU 逻辑核心数和系统盘剩余空间；只读，可自动执行。
- `open_application`：只能打开记事本、计算器、文件资源管理器或任务管理器。
- `open_folder`：只能打开当前用户的桌面、文档、下载或主目录；不接受路径字符串。
- `lock_workstation`：锁定当前 Windows 会话。

## 安全机制

- 电脑控制默认关闭，必须在设置中主动启用。
- 状态变更工具全部先显示中文操作预览，再由用户本机确认。
- 每个提案使用随机一次性令牌，120 秒后过期；执行或拒绝后立即失效。
- 工具参数使用 JSON Schema 枚举和 `additionalProperties: false`，未知工具、额外参数和任意路径全部拒绝。
- 执行层不使用 `shell=True`，没有 CMD、PowerShell、任意脚本或模型生成命令入口。
- 在线模型即使返回恶意工具名称，也只能得到“工具不在白名单”的错误，不能直接触发系统操作。

## 兼容性

- 只有支持 OpenAI-compatible `tool_calls` 的文本模型才能主动调用电脑工具。
- 不支持工具调用的本地模型继续正常聊天，不影响 0.5/0.6 的流式语音能力。
- 当前工具执行只支持 Windows 桌面版；浏览器模式和其他系统会显示不可用。

## 后续边界

文件创建、文件修改、发送消息、安装软件、付款、删除和任意自动化尚未开放。这些能力需要更细粒度的路径沙箱、内容预览、撤销策略和单独确认，不能复用“打开应用”的低风险权限。
